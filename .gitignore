# data artifacts — regenerable from the pipeline, never committed
data/*
cache/
.env

# ...except the hand-decided merge/split overrides, which are NOT regenerable.
# Losing this file means re-deciding every pair by hand (see docs/decisions.md).
!data/overrides.jsonl

# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# macos
.DS_Store

# original project spec — kept local, not published
rag-enterprise-data.md

# Postgres dumps — restore with pg_restore, see README. Big and regenerable-for-money.
backups/
